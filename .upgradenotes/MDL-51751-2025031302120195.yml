issueNumber: MDL-51751
notes:
  mod_quiz:
    - message: The function `question_usage_by_activity\add_question` has a new parameter slotnumber that allows
        us to add new question to the specfic slot, if it is empty, we will add the last slot.
      type: changed
    - message: Introducing a new function `quiz_attempt\get_missing_questions_on_usageid` to get the list of missing question.
        That current been in used by quiz.
      type: improved
    - message: Introducing a new function `quiz_attempt\check_for_and_replace_missing_question` to replace the missing question in the quiz attempt.
      type: improved
